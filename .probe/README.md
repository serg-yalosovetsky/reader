# Стенды хрома читалки

Замеры интерфейса читалки на реальном `frontend/index.html` в headless Chrome.
Лежат в репозитории намеренно: ими доказаны тач-цели и жесты в serg/tasks#403,
и следующая приёмка должна иметь возможность прогнать их сама, а не собирать
свои с нуля.

## Запуск

```sh
cd .probe && npm install          # puppeteer-core, локальный Chrome не качается
node measure.mjs ../frontend      # геометрия хрома и размеры тач-целей
node behave.mjs  ../frontend      # поведение: заголовок, меню, жесты, a11y
node fonts.mjs   ../frontend      # шрифты книги и уровни полей + миграция настроек
node scroll-anchor.mjs ../frontend  # «лента»: рост документа не отбрасывает прокрутку (#909)
```

Путь к Chrome берётся из `CHROME_PATH`, по умолчанию —
`C:/Program Files/Google/Chrome/Application/chrome.exe`.

`measure.mjs` завершается ненулевым кодом, если хоть одна цель меньше 44 px.

## Чем меряются тач-цели и почему именно так

Хит-тестом (`document.elementFromPoint` по обеим осям от центра элемента), а
НЕ геометрией бокса и не размером псевдоэлемента.

Это не педантизм: расхождение между двумя способами однажды дало в отчёте
«44 px» при фактических 34 px под пальцем. Невидимый `::before` со свесом
наружу был нарисован правильных размеров, но обрезался `overflow:hidden`
родителя и перекрывался соседним слоем. Бокс отвечает на вопрос «нарисован
ли элемент», хит-тест — «можно ли по нему попасть»; проверять надо второе.

## Проверка стенда на слепоту

Стенд, который зелёный и до, и после правки, ничего не доказывает. Оба стенда
проверялись так:

- `measure.mjs` на коммите `2594820` (до починки тач-целей) выдаёт 34 / 26 / 39
  вместо 44 — то есть дефект он видит;
- `behave.mjs`: со снятой проверкой `getSelection()` двойной тап при активном
  выделении включает полный экран (2 вызова вместо 1);
- `fonts.mjs` в конце проверяет себя дважды: заведомо несуществующим семейством
  и семейством с кириллицей, но БЕЗ курсива (Golos Text). Второе появилось не
  случайно: `document.fonts.check('italic ...')` отвечает `true` и для шрифта
  без italic-начертания, потому что Chrome синтезирует наклон, — настоящий
  курсив виден только перечислением `FontFace` со `style === 'italic'`. Первая версия этой проверки, к слову, валила все
  восемь рабочих шрифтов: `document.fonts.check()` отвечает `false`, пока файл
  не загружен лениво, поэтому перед проверкой нужен `document.fonts.load()`.

Делайте так же с каждой новой проверкой: сначала убедитесь, что она падает без
исправления.

## Прокрутка «ленты» и якорь (serg/tasks#909)

`scroll-anchor.mjs` поднимает настоящий `vendor/foliate-js/paginator.js` без
бэкенда: синтетическая глава, касания через CDP `Input.dispatchTouchEvent`,
«рост документа» — div в body главы (так делают ленивые картинки, шрифты,
перевод). На VPS Chrome — `CHROME_PATH=/root/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome`.

Проверка на слепоту: на коде до правки (305128d) сценарии 1–5 красные — палец
отпущен на 412 px, итог 343 px, а возврат к якорю сдвигал текст на строку.
Сценарии 4 и 5 красные по той же причине, поэтому чистыми регрессиями они не
являются. Сценарии 6 и 7 зелёные и до правки: они сторожат сохранение
прогресса и возврат к якорю навигации; сценарий 7 краснел на первом варианте
правки, где упор пальцем в край главы считался прокруткой.
